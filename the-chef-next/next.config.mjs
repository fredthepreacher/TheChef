/** @type {import('next').NextConfig} */
const nextConfig = {
  // Static-friendly: all pages are prerendered at build time.
  // No special config needed — Vercel auto-detects Next.js.
};

export default nextConfig;
